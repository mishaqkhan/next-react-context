import NavBar from '../components/navbar/Navbar';

function Home() {
    return <div>
        <NavBar />
        <p>Welcome to Next.js! This is nextJS testing ground to carry out various tasks in SSR environment that we do in CSR.</p>
    </div>
}

export default Home;